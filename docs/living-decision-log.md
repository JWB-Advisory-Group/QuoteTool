# Living Decision Log

This document is deliberately small. Update it when real-world signal changes the tactics.

| Review Point | Evidence Required | Decision |
| --- | --- | --- |
| After truth gate | 15-address truth gate result with address, parcel, sqft/year coverage | Keep property data optional unless coverage clears the bar |
| After first live quote | Customer source, form completion, Dante review friction | Fix intake before adding intelligence |
| After 5 real submissions | Source distribution and time-to-review | Move distribution ahead of polish if traffic is weak |
| After 5 explicit outcomes | Compliance %, explicit won amounts, quote overrides | Keep V1.5 blocked below 80% compliance |
| After 7-day soak | Quote count, outcomes, missed notifications, below-floor attempts | Decide whether AI summary can unlock |

Current defaults:

- Runtime pricing uses customer-reported job size, stories, urgency, conservative cost inputs, and manual LI competitor medians.
- No demographic fields are persisted.
- AI premium logic remains blocked until at least 10 quotes and 5 explicit won outcomes.
- Nassau enrichment remains absent unless the truth gate validates a stable public endpoint.
